/**
 * ----------
 * Auth Setup
 * ----------
 * 1. create firebase project
 * 2. enable web
 * 3. enable sign in method
 * 4. install firebase
 * 5. firebase.config.js file
 * 6. export app from firebase.config.js file
 *
 * ----------
 * Context
 * ----------
 * 1. create and export auth context
 * 2. Set auth context provider
 * 3. make sure you set the children
 * 4. value set
 * 5. in index.js file set import and set Context component and as children give <App/> component.
 * 6. get form data
 * 7. getAuth in the UserContext
 *
 * ----------
 * firebase hosting
 * ---------
 * 1. one time for each computer
 * 2. npm install -g firebase-tools
 * 3. firebase login
 * 4. firebase init (for each project one time )
 * 5. build folder select
 * 6. single page ? y
 * 7. github ? n
 *
 * *** for every delpoy
 * 1. npm run build
 * 2. firebase deploy
 */
